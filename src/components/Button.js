
const Button = ({name}) => {
  return (
    <div className = "">
        <button className = "bg-gray-200 py-1 px-2 rounded-lg mx-2 text-sm">{name}</button>
        
    </div>
  )
}

export default Button;